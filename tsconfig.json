{
  "compilerOptions": {
    "allowJs": true,

    "outDir": "./target-tsc",

    "baseUrl": "app/",
    // Note: it might be required to define those again in `jest.config.js`.
    "paths": {
      // START COPIED FROM `shared/tsconfig-base.json`
      "shared/*": ["../shared/*"],
      // END COPIED FROM `shared/tsconfig-base.json`

      "client/*": ["../client/*"],
      "server/*": ["../server/*"],
      "demos/*": ["../demos/*"]
    }
  },
  "files": ["./client/index.js"]
}
